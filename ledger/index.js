const erc20 = require('./erc20')

/**
 * A module for working with ERC20 and ERC721 ledgers.
 * @module ledger
 * @example
 * const ledger = require('tinyara/ledger')
 * const weth = ledger.erc20.weth()
 * const ara = ledger.erc20.ara()
 */
module.exports = {
  erc20
}
